version: '3.8'

services:
  gmail-client:
    build: .
    ports:
      - "12324:8000"
    volumes:
      - gmail_client_data:/root/.config/gmail-client
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    restart: unless-stopped

volumes:
  gmail_client_data:
