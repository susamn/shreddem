version: '3.8'

services:
  shreddem:
    build: .
    ports:
      - "12324:8000"
    volumes:
      - shreddem_data:/root/.config/shreddem
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    restart: unless-stopped

volumes:
  shreddem_data:
